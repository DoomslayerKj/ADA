#include<stdio.>

void swap(int *x, int *y){
    int temp = *x;
    *x = *y;
    *y = temp;
}

int k_element(int p[], int dir[], int n){
    int i,pre,min
    min = p[0];
    for(i = 0;i < n; i++){
        if(p[i] > min && dir[i] == dir[i+1])
    }
}
